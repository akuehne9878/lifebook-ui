specVersion: "1.0"
type: application
metadata:
  name: lifebook-ui

resources:
  configuration:
    paths:
      webapp: src/app

builder:
  resources:
    excludes:
      - "lib/**"
      - "openui5/**"
      - "index.js"

